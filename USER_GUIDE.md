# 愈心小筑 · 主理人一体化云端工作台 — 使用手册

> 适用对象：愈心小筑民宿主理人（小污）
> 项目目录：`G:\工作台`
> 核心数据源：腾讯文档【愈心小筑】入住情况（文件 ID `DYUdtRWlQTmxheW11`，与 `aGmEiPNlaymu` 同源）
> **v1.0 升级（2026-08-04）**：同步状态徽章 / 30 分钟轮询 / 自媒体风控双层过滤 + 替换建议 / 完整结构化复盘

---

## 一、工作台总览

工作台采用 **1 个首页总览 + 2 大核心板块 + 多个子模块** 结构：

| 板块 | 子模块 | 核心能力 |
|------|--------|----------|
| **首页总览** | 默认页 | 4 区卡片 + 周期汇报（含自媒体/健康/成长/下周建议维度）+ 卡片拖拽排序 |
| **生活管理中心** | 经济管家 | 资产总览 / 收支记账 / 消费结构图表 / 理财健康 / 预算管控 |
| | 健康管家 | 生理周期 / 体重管理 / **体检报告上传归档** / 保单管理 |
| | 个人成长 | 技能成长地图 / 读书管理（**AI 摘要/推荐**）/ 学习打卡 |
| | 设置 | **权限角色 / 字段可视化配置** |
| **工作经营中心** | **房态管理** | **套房分组日历 / 整分租锁房 / 自然语言操作 / 当日看板 / 订单明细 / 季年报** |
| | 民宿营销 | 房态概览 / 空置营销联动 / 营收成本 |
| | 自媒体中台 | 热点采集 / 选题转化 / 内容生成 / 合规风控 / 发布日历 |

数据沉淀：**生活数据本地浏览器 + 云端 KV 双轨**；**民宿业务只读腾讯文档 + 可靠写入云端 KV**。支持 PC / 移动端网页自适应。

---

## 二、多端访问与云端同步

### 登录
打开工作台首先进入**登录闸门**：输入云端账号 `1390343237@shturl.` 与访问口令（默认 `Yuxin2026!`）。账号即云端身份，用于跨设备数据隔离；口令仅做本地会话校验。

### 启用云端同步
民宿业务数据默认读取**真实腾讯文档**；生活数据（记账/健康/成长/订单/汇报）通过**云端 KV** 持久化，跨设备一致。云端同步依赖本地代理 `server/proxy.py`：

```bash
python server/proxy.py 8080
```

确保 `index.html` 中 `window.__SHEET_API__` 指向 `http://127.0.0.1:8080`（默认已配置），登录后即为实时云端模式。代理未运行 → 自动回退**本地模式**（民宿显示内置示例，生活数据存本机）。

### 同步状态徽章（v1 新增）
顶部导航栏右侧实时显示 4 种状态，鼠标悬停查看最近一次写回执：

| 状态 | 文案 | 颜色 | 含义 |
|------|------|------|------|
| `kv_doc_ok` | ✓ 已落 KV + 已落文档 | 🟢 绿 | 数据已可靠写入 KV，腾讯文档也同步成功 |
| `kv_only` | ✓ 落 KV · ⚠ 文档失败 | 🟠 橙 | 数据已落 KV，文档同步失败（不阻塞，可稍后人工校准） |
| `local_only` | ⚠ 仅本地（代理离线） | ⚪ 灰 | 代理未启动，所有写入仅存本机 |
| `syncing` | ↻ 同步中… | 🔵 蓝（脉冲） | 正在拉取云端数据 |

> **重要说明（数据源边界）**：当前腾讯文档 MCP 连接「读正常、写不落盘」，故所有新增/取消/调整等**可靠写入走云端 KV**；文档侧 5 个 Sheet 由系统自动建好结构，后台手动录入的数据前端可读取并随 30 分钟轮询刷新。这是对"文档为唯一写入底座"的必要技术偏离，已在代码中标注。

### 写入策略（v1 落定）
- **KV 为主**：所有「写」操作（订单、记账、健康、复盘、自媒体草稿）优先写入云端 KV。
- **文档尽力同步**：复盘、关键里程碑追加一行到腾讯文档「操作日志」sheet（best-effort，失败不影响主流程）。
- **轮询节奏**：每 30 分钟拉取一次腾讯文档最新内容到本地（避免过于频繁触发 API 限频）。
- **失败兜底**：所有写先入 KV 再尝试文档；文档失败时徽章转 `kv_only`，不阻塞业务。

---

## 三、房态管理（核心，`#/biz/rooms`）

### 1. 整分租互斥锁房规则（系统强制）
- **401 / 1502**：仅分租，按单间独立管控。
- **1302 / 407 / 503**：可整租也可分租，互斥锁房：
  - 生成「整租」订单 → 需该套房全部对应单间均空，否则**拦截并弹窗给出冲突原因**；整租成立后自动锁定全部子房。
  - 任一对应单间已有分租预定/在住 → 「整租」通道关闭。
- **1202**：纯整租，无分租单间。

新增/改期/续住/取消/入住/退房每一步都会**先校验锁房规则**，冲突一律阻止并给出明细。

### 2. 四种视图
- **套房分组日历（默认）**：未来 31 天 × 套房/子房状态矩阵，颜色区分 空置(绿)/已订(蓝)/在住(橙)/退打扫(灰)/整租占用(深灰)；点击单元格可新增订单或执行操作。
- **当日工作看板**：今日入住 / 退房 / 待打扫 / 空置，以及全量在住房间；一键办理入住、退房、标记打扫完成。
- **订单明细列表**：按房间/类型/状态/日期筛选；每行可 改期 / 取消 / 入住 / 退房；支持导出 CSV。
- **顶部总览卡片**：今日在住间数 / 今日入住率 / 今日新增订单 / 未来 7 天空置间数 / 本月累计营收。

### 3. 自然语言操作（指令栏）
在顶部指令栏输入自然语言，回车或点「执行」：
- `新增 407 套房整租订单 8月10日入住 8月12日退房 客人张先生 总价760元`
- `把 5 号房 8 月 15 日的订单续住到 8 月 18 日`
- `查询未来一周所有可售空房`
- `标记 8 号房打扫完成`

系统走「校验→落单→写日历→记日志→回结果」统一闭环，失败返回明确原因。

### 4. 数据绑定与同步
- 5 个 Sheet 由 `RoomState.ensureSheets()` 首次打开时自动创建：房源基础配置 / 订单主表 / 房态日历矩阵 / 操作日志 / 房态营收汇总。
- 预置 6 套房（401 仅分租 1-4、1302 整分 5-6、407 整分 7-8、1502 仅分租 9-12、503 整分 15-16、1202 纯整租）。
- 所有操作全程留痕至「操作日志」；可靠写入走云端 KV，跨设备可见。

### 5. 季 / 年报
在房态管理内可切换季度 / 年度口径，查看营收、间夜、入住率、各房型贡献与趋势（数据取自房态订单实时聚合）。

---

## 四、自媒体营销中台
- **热点榜单**：「抓取最新」拉取垂直领域热点；每日 7:00 自动化更新。
- **选题库**：热点自动转化为适配选题，标注侧重点与平台。
- **内容生成**：选中选题一键生成小红书文案 / 抖音脚本。
- **双层合规风控（v1 升级）**：
  - **医疗层**：内置医疗违规词库（疗效承诺、保证治愈、医院推荐等），命中后**用红底标出**并给出 3 条替换建议，支持**一键采纳**或**自动改写**。
  - **平台层**：内置小红书 / 抖音违禁词库（加微、最便宜、拼多多等），按平台单独过滤；命中后同样给替换建议。
  - 严重违规自动把内容状态置为「合规待审」，**不会直接丢弃**——主理人可改完再发布。
- **发布日历**：排期、状态、产出统计。

---

## 五、生活管理中心
- **经济管家**：账户资产结构 / 收支记账(手动+批量CSV) / 消费结构图表(占比环图·收支柱图·非必要透视) / 理财健康 / 预算预警。
- **健康管家**：生理周期(预测+提前提醒) / 体重(趋势+目标) / **体检报告文件上传**（图片/PDF，本地存储可预览删除）/ 保单(续保提醒)。
- **个人成长**：技能成长地图 / 读书管理（**AI 生成的真实摘要与偏好推荐**，无模型接口时显式标记"待接模型"）/ 学习打卡月报。
- **设置**：**权限角色**（主理人/家属/临时，控制敏感模块与操作）；**字段可视化配置**（房源/标签/预算等改为表单编辑）。

---

## 六、自动化（无需手动触发）

已在 WorkBuddy 创建 **8 个定时任务**：

| 任务 | 触发 | 产出 |
|------|------|------|
| 首页晨间日报 | 每日 08:00 | 当日排班/待办/复盘（云端 KV） |
| 首页周度总结 | 每周一 08:00 | 上周全维度周报+建议 |
| 每日热点 | 每日 07:00 | 垂直领域热点榜单 |
| 房态晨间日报 | 每日 08:00 | 房态当日概览+待办 |
| 房态数据巡检校准 | 每 2 小时 | 订单/日历/日志一致性校验 |
| 空置房营销提醒 | 每日 10:00 | 空置窗口+推广建议 |
| 周度房态营收汇总 | 每周一 09:00 | 上周营收/间夜/入住率 |
| 周日晚台账备份 | 每周日 23:00 | 全量数据备份 |

> 即使自动化未运行，首页/房态页也可手动点击即时生成；热点页「抓取最新」即可刷新。

### 复盘体系（v1 新增）
- **今日复盘**：每天 21:30 自动生成，按 5 段结构——【民宿】当日入住/退房/营收、【自媒体】发布条数与命中违规数、【生活】记账条数/预算偏离、【成长】读书与学习打卡、【明日待办】从订单自动推算。
- **本周复盘**：每周日 21:30 自动生成，同 5 段但拉到 7 天维度，附「下周建议」（从空置窗口、违规高频词、消费结构反推）。
- **手动触发**：首页「周期汇报」卡片下「重新生成今日/本周」按钮可即时跑。
- **留痕位置**：写入云端 KV `home/dailyReport` 与 `home/weeklyReport`；关键行追加到腾讯文档「操作日志」。

---

## 七、本地目录结构
```
G:\工作台\
├── index.html              # 单页应用入口
├── assets\css\app.css      # 自定义样式
├── assets\js\
│   ├── app.js              # 启动/路由/提醒/文档轮询
│   ├── lib\                # date/notify/store/router/sheet/lock/compliance/roomstate
│   └── modules\            # home/biz-bnb-rooms/biz-bnb/life-*/biz-media/settings
├── server\proxy.py         # 云端同步代理（腾讯文档读 + KV 写）
├── data\                   # 配置与自动化缓存
└── USER_GUIDE.md
```

---

## 八、常见问题
- **民宿日历空白？** 依赖云端代理；未配置时显示内置示例（mock）。接入真实文档需设 `window.__SHEET_API__` 指向代理。
- **后台改了文档前端没变？** 前端已做定时轮询刷新（30 分钟一次）；若代理未启动则读本地示例，需在代理模式下才读真实文档。
- **数据会丢吗？** 云端模式存 KV 跨设备；本地模式存当前浏览器，勿频繁清缓存。
- **锁房冲突？** 属正常拦截，按弹窗调整日期或房型即可。
- **权限/字段配置在哪？** 生活管理中心 → 设置。
- **同步徽章显示「落 KV · 文档失败」？** 正常现象——腾讯文档 MCP 写不可靠时降级为 KV 写入，文档侧可手动补录；不影响业务。
- **自媒体文案「合规待审」状态是什么？** v1 风控检测到医疗/平台违禁词后自动置此状态；点击「一键采纳」或「自动改写」后回到「草稿」即可正常发布。

---

*本工作台所有核心规则（整分租锁房、文档同步、合规风控、权限控制）强制生效，支持字段可视化与持续迭代。*
