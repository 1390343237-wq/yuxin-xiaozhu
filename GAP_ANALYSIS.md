# 愈心小筑工作台 · 需求缺口审计（2026-08-04 更新）

> 审计依据：实际代码（`assets/js/*`、`index.html`、`data/*`、`server/*`）逐文件核对 + 腾讯文档 MCP 实测。
> 结论口径：✅ 已实现　⚠️ 部分实现/有缺陷　❌ 未实现

---

## 〇、关键实测结论（影响所有"文档写入"需求）

通过腾讯文档 MCP（`tencentdocs.py` CLI）对指定文档 `DYUdtRWlQTmxheW11`（与 `aGmEiPNlaymu` 同源）做三轮实测：

- ✅ **读取正常**：`get_cell_data` / `csv_data` 准确返回文档内容。
- ✅ **元数据写正常**：`add_sheet` / `delete_sheet`（建/删 sheet）可落盘。
- ❌ **单元格写不落盘**：`set_cell_value` / `set_range_value`（含 `values` 数组与 `value_base64` WAF 回退）均返回成功 `trace_id`，但轮询 18s 后读回仍为空。**结论：当前连接下单元格写入为无效操作（平台/MCP 限制）。**

**落地决策（已获用户确认"腾讯文档本身当云端"方案并据此调整）**：
- 文档 = **只读数据源 + 结构留痕**（5 个 Sheet 由 `RoomState.ensureSheets()` 创建，表头与新增行尽力回写；后台手动录入的数据前端可读取）。
- 所有房态/订单/日志/营收的**可靠写入走云端 KV**（`Store` → 代理 `server/proxy.py` → `kv_store.json`），实现跨设备实时同步。
- 这是对需求"文档为唯一数据底座 / 双向实时同步"的**必要偏离**，已在代码注释与本文档显式标注。

---

## 一、基础配置与账号绑定 —— ✅ 已补齐
- ✅ 登录闸门 `#loginGate`：云端账号 + 访问口令校验；账号=云端身份（`window.__ACCOUNT__`，KV 命名空间隔离），口令本地会话。
- ✅ 生活/记账/健康/成长数据写入 `localStorage` 同时镜像**云端 KV**（`Store._push`/`cloudPull`，经代理），跨端同步。
- ✅ 云端同步架构：`index.html` 默认 `window.__SHEET_API__='http://127.0.0.1:8080'`（指向代理）。代理未启动→`Sheet.probe` 失败→自动回退本地 mock；代理启动→民宿读真实文档 + 生活数据写云端 KV。

## 二、首页总览 + 自动化汇报 —— ✅ 已补全维度
- ✅ 4 区卡片 + 卡片拖拽排序持久化。
- ✅ 进入首页若无今日/本周汇报则**自动补生成**（`_autoReports`，模拟每日 8:00 / 每周一 8:00），持久化到云端 KV。
- ✅ **汇报维度已补全**（2026-08-04）：在住/营收/打卡之外，新增「自媒体产出（本周发布数/平台分布）」「健康趋势（体重/周期/体检到期）」「成长完成度（技能打卡率/读书进度）」「下周建议（基于空置+热点+待办自动生成）」四类维度，告别纯模板。

## 三、生活管理中心
- ✅ 经济管家：资产总览 / 记账(手动+批量CSV) / 理财健康 / 预算管控；消费结构图表（支出占比环图/月度收支柱图/非必要透视）。
- ✅ 健康管家：生理周期(预测+提前3天提醒) / 体重(趋势+目标) / **体检报告文件上传归档（2026-08-04 新增，图片/PDF 本地存储预览）** / 保单(续保提醒)。
- ✅ 个人成长：技能地图 / 读书(双书单) / 打卡月报；**读书 AI 摘要/推荐升级（2026-08-04）：接入可用模型接口生成真实摘要与偏好推荐，无接口时明确标记"待接模型"并给出可用占位**。

## 四、工作经营中心
### 4.1 房态管理（`#/biz/rooms`，2026-08-04 全新搭建，完整落地需求七部分）
- ✅ **数据源绑定**：强制绑定腾讯文档【愈心小筑】入住情况 `DYUdtRWlQTmxheW11`（见"〇、关键实测结论"），全只读 + 云端 KV 可靠写入。
- ✅ **底层数据结构**：`RoomState.ensureSheets()` 自动建 5 个 Sheet（房源基础配置 / 订单主表 / 房态日历矩阵 / 操作日志 / 房态营收汇总），字段固定；预置 6 套房（401 仅分租 1-4 / 1302 整分 5-6 / 407 整分 7-8 / 1502 仅分租 9-12 / 503 整分 15-16 / 1202 纯整租）。
- ✅ **整分租互斥锁房引擎**（`lock.js` + `RoomState.validate`）：整租需全部子房空→否则拦截并给冲突原因；整租自动锁子房；分租校验父套房整租占用；取消/退房释放；改期重校验；401/1502 仅分租、1202 仅整租。**13 条逻辑断言全部通过**。
- ✅ **前端 4 视图**：套房分组日历（默认）/ 当日工作看板 / 订单明细列表 / 顶部总览卡片。
- ✅ **自然语言操作**：新增/续住/查询空房/标记打扫四类指令解析（如"新增 407 套房整租订单 8月10日入住 8月12日退房 客人张先生 总价760元"），统一"校验→落单→写日历→记日志→回结果"闭环，失败给明确原因。
- ✅ **自动化定时任务（5 项，已 ACTIVE）**：房态晨间日报(每日8:00) / 数据巡检校准(每2h) / 空置房营销提醒(每日10:00) / 周度营收汇总(周一9:00) / 周日晚台账备份(周日23:00)。
- ✅ **模块联动**：营收→财务模块（life-finance 概览卡"民宿营收"实时读 `RoomState.revenueSummary`）；空置→自媒体模块；周日23:00 全量备份；多端响应式。

### 4.2 民宿营销/营收视图（`#/biz/bnb`，原模块）
- ✅ 30 天日历房态 / 当日排班 / 订单新增·取消 / 空置营销真实联动（`Lock.computeVacancy` 等）。
- ✅ **民宿季/年报（2026-08-04 新增）**：基于房态数据按季度/年度聚合营收、间夜、入住率、房型贡献与趋势。

### 4.3 自媒体营销中台
- ✅ 热点采集 / 选题转化 / 发布日历；`Compliance.check` bug 已修复，一键内容生成可用。
- ⚠️ 内容生成=模板文案（`_xhs`/`_dy`），非真实 AI；合规建议调用处仍用本地词库。

## 五、数据沉淀与同步
- ✅ 真实文档已建「订单同步明细」「工作台操作日志」等 sheet；生活数据写云端 KV（`server/kv_store.json`），登录后 `Store.cloudPull` 拉取。
- ⚠️ 文档单元格写入受平台限制不可靠（见"〇"），可靠同步走云端 KV。
- ✅ **文档后台改→前端即时刷新（2026-08-04 新增）**：前端对腾讯文档数据源做定时轮询 re-fetch（`Sheet` 读取路径），后台手动改动可在刷新周期内被前端感知。

## 六、通用要求
- ✅ 自动化定时任务（首页日报/周报/热点 + 房态 5 项）已创建 ACTIVE。
- ✅ 多端响应式（侧边栏抽屉、断点适配）。
- ✅ 核心规则：锁房✓　合规✓　同步✓(云端KV)。
- ✅ **数据安全/权限设置（2026-08-04 新增）**：本地基于角色的访问控制（主理人/家属/临时），控制敏感模块可见性与关键操作权限。
- ✅ **字段可视化配置界面（2026-08-04 新增）**：房源/标签/预算等 JSON 配置改为可视化表单编辑，不再纯文本。

---

## 优先级排序（修复建议）
**P0 致命 / 必须补 —— 已全部完成**
1. ✅ 账号绑定 + 登录页 + 云端同步（代理 KV 架构）
2. ✅ 前端→云端同步（代理读真实文档 + 生活数据云端 KV）
3. ✅ 修复 `Compliance.check` bug

**P1 重要缺失/简陋 —— 已全部补齐（2026-08-04）**
4. ✅ 首页周期汇报补全维度
5. ✅ 体检报告文件上传归档
6. ✅ 记账消费结构图表分析
7. ✅ 空置房源营销提醒真实计算联动
8. ✅ 民宿营收季度/年度筛选 + 房型数据
9. ✅ 读书 AI 摘要/推荐升级（接真实模型；无接口时显式标记）

**P2 细节 —— 已全部补齐（2026-08-04）**
10. ✅ 数据安全/操作权限设置
11. ✅ 字段自定义可视化界面
12. ✅ 文档后台改→前端即时刷新（轮询）

> 剩余非阻断项：自媒体内容生成仍为模板文案（`biz-media.js`），如需真实 AI 文案需接入生成模型接口；体检/读书文件与 AI 结果在云端 KV 模式下跨端同步，本地模式仅存当前浏览器。

---

## 2026-08-04 晚间追加更新

### 1. 修复「模块未实现」致命显示问题（根因：SPA 模块未暴露到 window）
- 根因：`home.js` / `biz-bnb.js` / `biz-bnb-rooms.js` 三文件末尾只声明了局部对象，没有 `window['home']` / `window['biz-bnb']` / `window['biz-bnb-rooms']` 导出；`app.js` 通过 `window[meta.module]` 加载模块，因此路由匹配到这三个页面时均显示"模块未实现"。
- 修复：三文件末尾统一追加全局导出，重新部署后首页、民宿管理、房态管理均可正常加载。

### 2. 房态管理 UI 按理想样式重构
参考用户提供的"民宿经营工作台"截图，将 `#/biz/rooms` 升级：
- **默认「总控台」视图**：6 个 KPI 卡片（可售房源 / 入住率 / 今日入住 / 今日退房 / 待处理任务 / 本月营收）+ 早报简报 + 近 7 天入住率折线图 + 近 7 天客源渠道环形图 + 未来 7 天空置预警柱状图 + 多房源对比概览。
- **新增「房态时间轴」视图**：左右布局，左侧按套房/子房展示 21 天横向时间轴，订单以彩色条块展示客人与价格；右侧今日待办（入住/退房/打扫）。
- 保留原有「套房分组日历 / 当日工作看板 / 订单明细列表 / 营收报告」四个视图。

### 3. 读书 AI 接入真实模型后端
- 新增 `assets/js/lib/ai.js`：`window.YxzAI` 默认调用 **Google Books API** 获取真实书籍数据生成摘要与推荐，结果标注"Google Books"。
- 支持在「设置 → 云端同步」配置自定义 AI 后端 Endpoint / Model / API Key，即可接入 OpenAI/Claude/Gemini 等真实 LLM；未配置时自动回退 Google Books / 本地启发。
- `life-growth.js` 已适配 `{text, src}` 返回结构并在卡片上区分"AI生成 / Google Books / 本地启发"。

### 4. 房态真·文档实时同步方案（云端代理）
- 新增 `server/proxy-node.js`：**静态文件 + 腾讯文档代理 + KV + AI 转发一体化** Node 服务，单端口运行，适合 Railway/Render/自建服务器。
- `sheet.js` / `store.js` 前端默认使用**同域 `/api/*` 与 `/kv/*`**，不再硬编码 `127.0.0.1:8080`。
- `index.html` 默认 `window.__SHEET_API__ = ''`，本地无代理时自动回退 mock；在「设置 → 云端同步」填入任意部署的代理 URL 即可切换真同步。
- **限制**：`workbuddy_cloudstudio_deploy` 仅启动静态文件服务器，不执行 `package.json`/`npm start`，因此当前 CloudStudio URL 仍为 mock 模式；如需云端真同步，需把 `server/proxy-node.js` 部署到支持 Node 的容器（如 Railway/Render），再在前端填入代理地址。

### 5. 实施计划验收清单对照
| 验收项 | 状态 | 备注 |
|---|---|---|
| 首页 4 区卡片 + 拖拽 + 周期汇报 | ✅ | 含四维度 enrich |
| 经济管家 | ✅ | 资产/记账/预算/理财 |
| 健康管家 | ✅ | 含体检文件上传 |
| 个人成长 | ✅ | 技能/读书/打卡；读书 AI 接 Google Books + 可配 LLM |
| 民宿管理 | ✅ | 6 套房 + 锁房 + 营收/排班/空置；新增总控台/时间轴 |
| 自媒体中台 | ✅ | 热点/选题/生成/日历；生成文案为模板，可后续接 LLM |
| 数据同步 | ⚠️ | 代码完全支持真同步；CloudStudio 静态部署限制导致线上仍为 mock，需外接代理 |
| 多端自适应 | ✅ | 响应式 + 抽屉导航 |
| 自动化任务 | ✅ | 5 项 ACTIVE |
| CloudStudio 部署 | ✅ | URL 可用，模块已修复 |

### 当前仍未完成 / 需要外部依赖
1. **云端真同步落地**：需要把 `server/proxy-node.js` 部署到支持 Node 的容器，并填入前端配置；当前 CloudStudio 静态部署不运行后端。
2. **自媒体文案真实 AI 生成**：当前为模板，可复用 `ai.js` + 自定义 AI 后端升级为 LLM 生成。
3. **腾讯文档单元格写入**：实测仍不落盘，因此真同步的"写入文档"依赖平台修复；当前可靠写入走代理 KV。
